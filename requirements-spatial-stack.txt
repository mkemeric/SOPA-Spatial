# Pinned spatial data stack — versions validated on the working remote
# environment (2026-03-13).  The spatialdata ecosystem is fragile across
# zarr/dask/ome-zarr version boundaries.  Do NOT upgrade these packages
# individually — test the full pipeline before changing any pin.
#
# To regenerate from a known-good environment:
#   pip freeze > requirements-lock-full.txt
#   # Then extract the packages below from the full freeze.

# Core spatial data
spatialdata==0.7.2
spatialdata-io==0.6.0
spatialdata-plot==0.2.14

# Sopa pipeline
sopa==2.2.1

# Storage / IO (tightly coupled)
zarr==3.1.5
ome-zarr==0.13.0

# Compute layer
dask==2026.1.1
numpy==2.4.3

# Single-cell
anndata==0.12.10
scanpy==1.11.1
squidpy==1.6.2

# Segmentation
cellpose==3.1.1.2
